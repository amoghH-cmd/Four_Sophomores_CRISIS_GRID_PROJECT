{
  "name": "crisis-grid",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently --names \"SERVER,CLIENT\" --prefix-colors \"red,cyan\" \"npm run dev --prefix server\" \"npm start --prefix client\"",
    "start:server": "npm start --prefix server",
    "start:client": "npm start --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "localtunnel": "^2.0.2"
  }
}
